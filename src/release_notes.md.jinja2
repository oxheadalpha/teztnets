Teztnets
========

{{ network }}
---------

To join {{ network }}, download the last master branch build of tezos-node then run:

```
tezos-node config init --network https://tqtezos.github.io/teztnets/{{ network }}
tezos-node run
```

|-------|---------------------|
| Baker | `tezos-baker-alpha` |

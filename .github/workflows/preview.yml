name: Pulumi Preview
on:
  pull_request:
    branches:
      - main
jobs:
  preview:
    name: Preview
    uses: ./.github/workflows/setup.yml
    secrets: inherit
    with:
      pulumi-command: preview
